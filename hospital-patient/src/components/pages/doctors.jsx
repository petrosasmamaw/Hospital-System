import React from "react";

export default function Doctors() {
	return (
		<div className="page">
			<h1>Doctors (Patient)</h1>
			<p>Example paragraph for the Patient Doctors page — used to verify routing and layout.</p>
		</div>
	);
}
