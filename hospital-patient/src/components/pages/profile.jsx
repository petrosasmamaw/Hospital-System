import React from "react";

export default function Profile() {
    return (
        <div className="page">
            <h1>Profile (Patient)</h1>
            <p>Example paragraph for the Patient Profile page — used to verify routing.</p>
        </div>
    );
}
