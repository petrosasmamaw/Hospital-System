import React from "react";

export default function Dashboard() {
	return (
		<div className="page">
			<h1>Dashboard (Admin)</h1>
			<p>Example content for the Admin Dashboard — visible when the route renders.</p>
		</div>
	);
}
