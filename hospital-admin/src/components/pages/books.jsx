import React from "react";

export default function Books() {
	return (
		<div className="page">
			<h1>Books (Admin)</h1>
			<p>This is an example paragraph for the Admin Books page — use it to verify routing and layout.</p>
		</div>
	);
}
