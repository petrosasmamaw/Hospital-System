import React from "react";

export default function BooksList() {
	return (
		<div className="page">
			<h1>Books List (Doctor)</h1>
			<p>Example paragraph for the Doctor Books List page — used to verify routing.</p>
		</div>
	);
}
