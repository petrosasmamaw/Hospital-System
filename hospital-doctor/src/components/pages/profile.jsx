import React from "react";

export default function Profile() {
    return (
        <div className="page">
            <h1>Profile (Doctor)</h1>
            <p>Example paragraph for the Doctor Profile page — used to verify routing.</p>
        </div>
    );
}
